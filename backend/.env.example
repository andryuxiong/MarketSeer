# MarketSeer Backend Environment Variables

# Required API Keys
FINNHUB_API_KEY=your_finnhub_api_key_here
IEX_CLOUD_API_KEY=your_iex_cloud_api_key_here

# Optional: Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Optional: Server Configuration
PORT=8000
HOST=0.0.0.0
NODE_ENV=development

# API Key Sources:
# - Finnhub: https://finnhub.io/ (free tier: 60 calls/minute)
# - IEX Cloud: https://iexcloud.io/ (free tier: 50k requests/month)
#   Note: IEX Cloud will use sandbox/demo mode if no key provided

# For production deployment:
# - Set FRONTEND_URL to your production frontend URL
# - Set NODE_ENV=production
# - Ensure both API keys are configured